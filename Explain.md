# 每日阿拉伯语单词打卡 · 使用说明

**文件：`daily-arabic.html`** —— 单文件网页工具，无依赖、无需联网、双击即可运行。
数据全部保存在本机浏览器（localStorage），刷新、关闭浏览器都不丢。

**已内置《词汇表_乱序.doc》的 932 个词条**（含 120 个原文标 `*` 的补充词），打开就能直接背。

---

## 一、三个分区：阿译中 / 中译阿 / 默写区 ⭐

**背诵区**下面分成两个子分区（页面上是并排的两个按钮，点一下切换）：

| 分区 | 干什么 | 作答方式 |
|---|---|---|
| 📖 **背诵区 · 阿 → 中** | 认读：看阿语想中文 | 点「显示释义」看答案 → 自评 |
| 📖 **背诵区 · 中 → 阿** | 回想：看中文想阿语 | 点「显示释义」看标准答案 → 自评 |
| ✍️ **默写区** | 打字：输入中文释义或阿拉伯语 | 输入框打字 → 自动判定 |

- 两个子分区**共享背诵区的设置**（每天新学多少、抽查比例、分配比例），但**进度各算各的**：
  阿→中 记住了 5 个，中→阿 可能一个都还没做。
- 今天的词怎么分到两个子分区？由「**分配比例**」决定（默认 50%：今天的 10 个词 → 阿→中 5 个、中→阿 5 个，**总量不变**）。
  想两个方向练同一批词，把「中→阿 的内容」切成 **🔗 跟练阿→中**（见第二节）。
- **建议顺序**：先做「阿→中」（先认识），再做「中→阿」（再回想），最后去默写区打字 —— 默写区默认就跟这两批词同步。
- **学习闭环**：默写区默认**与背诵区同步** —— 两个子分区今天背过的词（合起来去重）就是默写内容；
  没背完时默写区顶部会提示还剩几张，点一下就能跳过去。
- **打卡**：三个分区都完成（或某分区无任务）才能打卡，打卡页会分别列出三个分区的状态。

---

## 一之二、进度池：多人 / 多份进度各存一份 ⭐

顶部有一个 **👤 进度池** 选择器。每个池子 = **一整套独立数据**（含下面说的「单词本」）：

| 池子里存什么 | 说明 |
|---|---|
| 学习进度 | 每个词的记忆等级、上次复习时间、下次复习日期、忘记次数；三个分区各自的队列与判定 |
| 背诵习惯 | 每日新词量 / 复习抽查比例 / **分配比例**、中→阿 是否跟练、默写区是否同步 |
| 打卡记录 | 连续天数、历史记录、默写写对率 |
| 单词本 | 池子里的词表，可以有**好几本**（内置 932 词 / 自己导入的），见「一之三」 |

**首次打开**会弹一个小窗，让你先给自己的池子起个名字（比如「小明」「上班地铁背」）——名字只是本机上的标签，
数据始终只存在这台设备里。之后在顶部随时切换。

| 操作 | 在哪 | 说明 |
|---|---|---|
| 切换 | 顶部选择器 → 选另一个池 | 立刻换成那个人的进度，互不干扰 |
| ＋ 新建 | 顶部「＋ 新建」 | 建一个全新空池（自动补齐内置 932 词，进度从零开始） |
| 改名 / 复制 / 删除 | 设置页「👤 进度池」卡片，或列表里每个池子后面的按钮 | 复制 = 深拷贝一份，之后两边各走各的；删除不可恢复 |
| 导出当前池 | 设置页「导出当前池（JSON）」 | 填进下方输入框并尝试复制到剪贴板，换设备时用它 |
| 导入为新池 | 设置页粘贴 JSON → 「粘贴导入为新池」 | 不会覆盖现有池子，而是新建一个池 |

**一台设备放几个池子合适？** 上限 20 个；每个池子约 150 KB，普通浏览器（5 MB 上限）放十几个没问题。
列表里会显示每个池子的单词数、背过多少、打卡天数、占用大小。

> 小提示：数据按「浏览器 + 网址」隔离。从本地文件（`file://`）打开时的池子，**不会**出现在 GitHub Pages 网址上，
> 迁移请用「导出当前池 → 复制 JSON → 在新设备粘贴导入为新池」。

---

## 一之三、单词本：一个池子里可以放好几本词表 ⭐

「📚 单词本」页最上面是**单词本管理**。一个单词本 = 一份独立的词表 + 每个词自己的记忆进度：

| 操作 | 说明 |
|---|---|
| **切换** | 下拉框选另一本，或在列表里点「切换」。切换后会**按新词表重排今天的任务** |
| **＋ 新建空单词本** | 从零开始：新建后自己粘贴导入（比如四级词、课文生词） |
| **＋ 新建（含内置 932 词）** | 想要一份「干净的默认词表」时用，进度从零开始 |
| **当前本改名 / 复制 / 删除** | 复制 = 词表照搬、**进度清零**（id 会重新分配，两本互不影响）；删除会连进度一起删 |
| **导出为文本** | 导出成 `单词 : 释义` 纯文本（带 `*` 补充词标记、可带音标列），可复制到记事本编辑 |
| **导入到当前单词本** | 粘贴 `单词 : 释义` → 合并进当前本（已存在的词只更新释义） |
| **导入为新建单词本** | 把粘贴的整份词表变成**一本新词表**并切过去 |

- 切换单词本后，背诵区顶部会显示当前是哪一本（`已记住 3/5 · 📚 内置词汇表`）。
- **进度是跟着词走的**：A 本背过的词，进度留在 A 本；切到 B 本看到的是干净的进度，切回 A 本进度还在。
- 「导出备份」（整池 JSON）里**包含所有单词本**；恢复后单词本和进度都在。
- 打卡记录、滑块设置是**池子级**的（整池共用），不随单词本切换而改变。
- 上限 30 本（想改就搜 `state.books.length >= 30`）。

> 和「进度池」的区别：**进度池 = 一个人的全部数据（单词本 + 习惯 + 打卡 + 历史）**，
> **单词本 = 池子里的一本词表**。多人共用一台设备用进度池；一个人想同时背几套词表用单词本。

---

## 二、背诵区的两个子分区怎么分

两个子分区**共用一份设置**（改一个另一个也跟着变），页面顶部按钮上会显示各自的进度角标（如 `· 3/5（剩 2）`）。

**① 按比例分配（默认）**：今天的词按「分配比例」切开

```
背诵·阿→中：新词 10 · 抽查 20% · 分配 50%
────────────────────────────────────────────
中→阿 的内容                     按比例分配
[🔗 跟练阿→中（同一批词）] [🎲 按比例分配]

每天新学单词                     10 个
[———●———————————————]  0 ────────────── 60

复习抽查比例（从已学的 300 个词里随机抽）   20%
[————●——————————————]  0 ───────────── 100%

分配比例（阿→中 : 中→阿）   阿→中 50% · 中→阿 50%
[——————●————————————]  0 ───────────── 100%

背诵·阿→中 今天预计 5 个：新词 5 个
背诵区今天共 10 个，分配：阿→中 5 · 中→阿 5
方向：阿译汉 5 个 · 汉译阿 0 个
```

| 设置 | 作用 |
|---|---|
| **每天新学单词**（0–60） | **整个背诵区**每天抽几个词；优先抽「今天在另一个区刚背过的词」，剩下名额给从未背过的词 |
| **复习抽查比例**（0–100%） | 从已学的词里随机抽百分之几来复习；名额内先满足按记忆曲线到期的词 |
| **分配比例**（0–100%） | 今天的词里，多少比例放「阿→中」、剩下的放「中→阿」。50% → 各一半；100% → 全部阿→中（中→阿 当天无任务） |
| **中→阿 的内容** | `🎲 按比例分配`（默认，总量不变）/ `🔗 跟练阿→中`（同一批词反过来再练一遍，总量 ×2） |

**② 跟练阿→中**：适合「一个词想彻底记住」的用法

```
阿→中：练今天的全部 10 个词 → 中→阿：再用同一批 10 个词反过来练 → 默写区：还是这 10 个词打字
```

- 开启后，「分配比例」滑块会自动收起（此时两个子分区都是全部词，比例没有意义）。
- 中→阿 顶部会提示：「🔗 跟练模式：这里是阿→中今天练过的那批词（还剩 N 张没背完）」。
- 切成跟练会让当天任务量翻倍（阿→中 10 + 中→阿 10 + 默写 10），当天已经判定过的卡不会丢。

**默写区**默认与背诵区同步；它的面板（设置页「✍️ 默写区」）长这样：

```
默写区：新词 10 · 抽查 20% · 分配 0%
────────────────────────────────────────────
默写内容来源                     同步背诵区
[🔗 同步背诵区（推荐）] [🎲 独立抽词]
...
```

| 设置 | 作用 |
|---|---|
| **默写内容来源** | `🔗 同步背诵区`（默认）= 默写背诵区今天背过的那批词（两个子分区合起来去重）；`🎲 独立抽词` = 按下面的滑块自己抽 |
| **题目方向**（仅默写区） | 默写时阿译中 / 中译阿的比例，默认 0%（全汉→阿 打字） |

> 想要「背诵区只看阿语想中文、默写区只看中文写阿语」？把「中→阿 的内容」设为按比例分配 + 分配比例 100%、
> 默写区方向拖到 0% 即可（这也是默认值）。

**复习节奏**：`✅ 记住了` → 等级 +1，按 **1 / 2 / 3 / 7 / 14 / 30 / 60 天**安排下次复习；`❌ 没记住` → 立刻回到本区队尾重背（当天最多 3 次），并记为「重点词」**同时进入三个分区**的第二天任务最前面（每个分区最多 60 个）。

每天抽到的词由「日期 + 随机种子」决定，**同一天刷新页面顺序不变**，第二天自动换一批。

---

## 三、卡片长什么样

| 分区 | 方向 | 卡片 |
|---|---|---|
| 📖 背诵区 · 阿 → 中 | 阿语 → 中文 | 大字显示阿语 → 点「显示释义」看中文 |
| 📖 背诵区 · 中 → 阿 | 中文 → 阿语 | 显示中文 → 想阿语 → 点「显示释义」看标准答案 |
| ✍️ 默写区 | 中文 → 阿语（默认） | 显示中文 → **输入阿拉伯语**，自动判定 |

### 认读卡片（背诵区两个子分区）

```
│ 复习 · Lv.2  阿 → 中   5 / 10 │
│          نَقَصَهُ             │  ← 阿译中：大字词头（点击朗读）
│  يَنْقُصُ نَقْصًا الشَّيْءُ      │  ← 补充形式（变位/复数/搭配）
│      [ 显示释义 👀 ]          │
│  释义：缺少，缺乏              │
│  完整词条                     │
│  [✅ 记住了]  [❌ 没记住]      │
```

（「中 → 阿」子分区的题面是中文，点「显示释义」看阿语标准答案，其余一样。）

### 默写卡片（默写区）

```
│ 新单词  中 → 阿（默写） 2 / 5 │
│        缺少，缺乏             │  ← 题面（中译阿时不会露出阿语）
│  ┌──────────────────────┐  │
│  │ 在这里输入阿拉伯语…     │  │  ← 回车提交
│  └──────────────────────┘  │
│  [ 提交默写 ⏎ ] [ 不会，看答案 ]│
│  [ 阿语键盘 ⌨️ ]              │  ← 没装阿语输入法也能打
│  ── 提交后 ──────────────── │
│  ⚠️ 差一点                    │
│  你的答案：نقصه  标准答案：نَقَصَهُ │
│  [ ✍️ 再写一次 ]              │
│  释义 + 完整词条              │
│  [✅ 记住了]  [❌ 没记住]      │  ← 推荐按钮自动高亮
```

**判定规则（宽松，只提示不强制）**：

| 情况 | 判定 |
|---|---|
| 阿语：不带音符、字母变体不同（أ/إ/آ→ا、ة→ه、ى→ي、ؤ/ئ）、多写了变位/复数 | ✅ 写对了 |
| 阿语：差 1–2 个字母 / 只写词干 | ⚠️ 差一点 |
| 中文：与释义或**任一义项**相同（答「缺乏」而释义是「缺少，缺乏」）、多写少写一个字 | ✅ / ⚠️ |
| 完全不沾边 | ❌ 写错了（给标准答案，可「再写一次」） |

最终仍由你点【记住了】/【没记住】——系统只给提示并把推荐按钮高亮。
**阿语屏幕键盘**：36 个字母键 + 空格 + 删除，电脑手机都能用。
**成绩**：默认区结算页显示「默写 N 题，一次写对率 X%」，打卡页累计统计。

**键盘快捷键**：`← →` 翻上/下一张，`空格` 显示释义，`1` 记住了，`2` 没记住（默写时 `回车` = 提交）。

### 卡片下方：上一个 / 下一个 导航 ⭐

每张卡片底部都有一行导航，可以**自由翻阅今天的所有卡片**（不必按顺序被推着走）：

```
┌────────────────────────────────────────────┐
│  [ ← 上一个 ]   第 3 / 10 张   [ 下一个 → ]  │
└────────────────────────────────────────────┘
```

- **下一个 →**：跳到下一张。**不判定也算跳过**，这张之后还会再轮到你，适合「先跳过、待会儿再背」。
- **← 上一个**：回到上一张；已判定过的卡片会**直接展开答案**并标注「已判定：记住了 / 没记住」
  （只读、不可改判），方便回头复习刚才那个词。
- 到第一张 / 最后一张时按钮会**变浅但仍可点**，点了会明确提示：「已经是第一张了」；
  在最后一张点「下一个」时，如果前面还有没判定的卡，会**自动跳回那张**并提示还剩几张
  （不会出现「点了没反应」的卡住感）。
- 判定（记住了 / 没记住）后光标会**自动跳到下一张还没判定的卡**；「没记住」的卡本轮先跳过，
  稍后绕回来再问（每天最多 3 次），全部问完才显示结算页。
- 结算页上有「🔁 回顾今天的词」，可以重新翻看今天所有卡片（含已判定的）。
- 三个分区的翻页按钮、评分按钮、键盘快捷键都**只作用于当前所在的分区**，不会串台。

---

## 四、打卡规则与记录

- 条件：**三个分区都背完**（把「没记住」的词重背完）；某分区词量设为 0（或分配比例把它排空）则视为无任务。
- 「✅ 打卡」页显示：三个分区各自进度（✅ 完成 / ⏳ 进行中 / — 无任务，带「去完成」按钮）、
  连续打卡天数、累计天数、最长连续、单词总数、累计记住次数、**默写一次写对率**、
  最近 28 天日历、历史打卡记录（含每天的背诵数/默写数/默写写对数）。

---

## 五、单词导入格式（可选，词表已内置）

每行一个，**单词 : 释义**（中文冒号 `：`、英文冒号 `:`、制表符都认）：

```
مَبَسَّطٌ : 简化的，通俗的
مُتَعَلِّمٌ جـ مُتَعَلِّمُونَ : 学生，学习者
*ثَقَافَةٌ : 文化                    ← 行首 * = 补充词，卡片上标 ＊
جَمِيلٌ::美丽的::jamīl               ← 高级格式 单词::释义::音标
```

**词条里的语法标记会自动拆成小字显示**：`جـ`（复数）/ `م`（阴性）/ 动词现在式 / ` / `（并列说法）
→ 大字只显示词头，其余作为「补充形式」小字，展开答案后有「完整词条」对照。

只有单词没释义的行、`#`/`//` 开头的行会被忽略；已存在的词只更新释义，不重复添加。
导入后新词会补进**当前单词本**，并自动补进三个分区今天的任务（保留已背记录）。

---

## 六、数据保存 / 备份 / 换设备

- 数据存在浏览器 localStorage：
  `arabicDaily.pools.v1` = 所有进度池；`arabicDaily.v1` = 当前启用池的镜像（旧版本 / 回退时也能读到）。
  **同一浏览器 + 同一文件路径**打开才看得到旧数据。
- 清理缓存 / 无痕模式 → 数据会丢；换浏览器、换电脑需要重新导入。
- **备份**（单池）：单词本 → 「导出备份」→ 当前池数据（JSON）填入输入框并复制，粘到记事本保存。
- **恢复**：把 JSON **原样粘贴**到单词本输入框 → 「导入 / 更新」，单词、进度、打卡记录一起恢复。
- **备份（整池搬家 / 不动现有数据）**：设置 → 「导出当前池」→ 复制 → 在新设备设置页粘贴 → 「粘贴导入为新池」。
- 旧版本数据会自动迁移：原来的「每日单词数量 / 抽查比例 / 方向 / 默写比例」会转换成背诵区与默写区的对应设置，
  并整体放进第一个进度池（名字可以在首次弹窗里改）。

---

## 六之二、上传到 GitHub（在线使用 / 分享）

**本项目的在线地址：<https://lcyxc.github.io/arabic-daily/>**（仓库 <https://github.com/lcyxc/arabic-daily>，main 分支根目录）
→ 本地改了 `daily-arabic.html` 后，把文件重新上传/推送一次，线上版就会更新（约 1 分钟生效）。

仓库里需要的文件：`index.html`（入口，自动跳转到工具）、`daily-arabic.html`（工具本体）、
`README.md`、`Explain.md`、`input.txt`。

**方式一：网页上传（不用命令行）**

1. GitHub 右上角 `+` → `New repository` → 填名字（如 `arabic-daily`）→ 选 Public → `Create repository`
2. 页面里点 `uploading an existing file` → 把上面几个文件拖进去 → `Commit changes`
3. 仓库 `Settings` → 左侧 `Pages` → Source 选 `Deploy from a branch`，Branch 选 `main` + `/ (root)` → `Save`
4. 等 1 分钟左右，访问 **`https://<用户名>.github.io/<仓库名>/`** 就是你的在线版
   （直接分享 `…/<仓库名>/daily-arabic.html` 也能用）

**方式二：命令行（Git）**

```bash
cd 到存放这些文件的目录
git init
git add index.html daily-arabic.html README.md Explain.md input.txt
git commit -m "阿拉伯语每日单词打卡工具"
git branch -M main
git remote add origin https://github.com/<用户名>/<仓库名>.git
git push -u origin main
```

> `git push` 提示要密码时，GitHub 现在需要 **Personal Access Token**（Settings → Developer settings →
> Personal access tokens 生成后当密码用），或改用 GitHub Desktop / VS Code 登录后推送。

**⚠️ 数据不跟着走**：学习记录存在浏览器 localStorage，按「来源」隔离。
从本地文件（`file://`）打开时的记录**不会**出现在 GitHub Pages 网址上，反过来也一样。
迁移方法：设置 →「导出当前池」复制 JSON → 在线页面设置页 → 「粘贴导入为新池」。

**私有仓库**：免费账号的 GitHub Pages 只能从 **Public** 仓库发布；不想公开就用 Private 仓库 + 本地打开文件。

---

## 七、内置词汇表（932 词）

- 首次打开自动载入全部 932 词；本机已有旧数据时**自动补齐**缺少的词并**刷新被修正过的词条**（保留进度）。
- 设置 → 「载入内置词汇表」可手动补齐（幂等）。删掉的词不会自己回来。
- 改词表：搜 `const BUILTIN_WORDS`（932 个 `["阿语","释义",是否补充词]`），改完把 `BUILTIN_VERSION` 加 1。

**已修正的 26 处原文笔误**（`ثَمَانِيمِائَةٍ`→`ثَمَانِي مِائَةٍ`、`إِعْطَاعًا`→`إِعْطَاءً`、`حَتًّى`→`حَتَّى`、
`يُدَربِّرُ`→`يُدَبِّرُ`、`أَمْسَامٌ`→`أَجْسَامٌ`、`سَلاَجحِفُ`→`سَلاَحِفُ`、`مَتَجِرُ`→`مَتَاجِرُ`、
`مُتَطَلَّبَتٌ`→`مُتَطَلَّبَاتٌ`、`تَفَصُّحًا`→`تَصَفُّحًا`、`إِشْرَقًا`→`إِشْرَاقًا`、`رَقِّى`→`رَقَّى`、
`فُلاَيًا`→`فُلاَنًا`、`إِبْعًادًا`→`إِبْعَادًا`、`الْوَحْدَةُ`→`الْوَاحِدَةُ`、以及多处音符错乱），
用阿拉伯语正字法规则扫描后复检 **0 处残留**。

---

## 八、常见问题

**Q：怎么只背单词不默写？**
A：把「默写区」切到「🎲 独立抽词」并把每日新词量调到 0；或者只待在「📖 背诵区」。

**Q：默写区和背诵区会不会各抽各的？**
A：默认不会——默写区默认**同步背诵区**：背诵区今天背哪些词，默写区就默写哪些词（顺序一致），
适合「背完立刻默写」的闭环。想各抽各的，把默写区切到「🎲 独立抽词」（这时它的词量/抽查比例滑块才会生效）。

**Q：两个人（或我和我的另一个学习计划）共用一台电脑怎么分开？**
A：顶部「👤 进度池」新建一个各自的池子，进度、习惯、打卡记录、单词本全都互相独立；随时切换。

**Q：换了台电脑 / 换了浏览器，进度带不过去？**
A：数据按「浏览器 + 网址」隔离。用设置页「导出当前池」复制 JSON，在新设备上「粘贴导入为新池」即可。

**Q：我想再建一份单独的词表（比如把四级词和课本词分开）？**
A：「📚 单词本」页 → 「＋ 新建空单词本」→ 起名 → 在下面的输入框粘贴 `单词 : 释义` → 点「导入到当前单词本」。
进度两本各算各的，顶部会显示当前用的是哪一本。

**Q：手机上用起来怎么样？**
A：已按手机优化：按钮/输入框都是「一指能点」的尺寸、输入框 16px（iOS 聚焦不会把页面放大）、
阿语键盘按屏宽自动换行、支持 iPhone 刘海与底部横条安全区、极窄屏会收掉次要文字。

**Q：想只练中译阿（看中文写阿语）？**
A：把「分配比例」拖到 0%（今天的词全给「中 → 阿」子分区）；默写区的「题目方向」本来就是 0%。

**Q：没有阿拉伯语输入法？**
A：默写时点「阿语键盘 ⌨️」用屏幕键盘点字母即可。

**Q：写对了却说错？**
A：判定已很宽松（忽略音符、字母变体、多写变位；中文命中任一义项即算对；差 1–2 字母提示「差一点」），最终由你点【记住了】/【没记住】。

**Q：打卡按钮是灰的？**
A：还有区没背完。打卡页会显示是哪个区、还剩几个；某区不想做就把它的词量调到 0。

**Q：每天想多背 / 少背？**
A：直接拖对应区的滑块，松手就重排该区今天的任务，已背记录不丢。

**Q：点了单词没有发音？**
A：需要系统安装阿拉伯语语音包（Windows：设置 → 时间和语言 → 语音 → 添加语音）。

**Q：数据会不会上传？**
A：不会。纯本地单文件，没有任何网络请求。

---

## 九、自定义小技巧

| 想改什么 | 怎么做 |
|---|---|
| 背诵区默认词量 / 分配比例 | 改 `defaultState()` 里 `read` 的 `newPerDay` / `ratio` / `a2cRatio` |
| 默写区是否默认同步 | 改 `defaultState()` 里 `write.sync`（`true` 同步 / `false` 独立抽词） |
| 复习间隔天数 | 搜索 `SPACE_DAYS` |
| 单区任务上限 | 搜索 `plan.slice(0, 250)` 里的 250（重点词上限搜索 `slice(0, 60)`） |
| 默写判定宽松度 | 搜索 `function gradeArabic` / `function gradeChinese`（`d <= 2`、`sim >= 0.6`） |
| 阿语屏幕键盘字母 | 搜索 `const AR_KEYS` |
| 进度池数量上限 | 搜索 `const MAX_POOLS`（默认 20） |
| 进度池默认名字 | 搜索 `const DEFAULT_POOL_NAME` |
| 内置词汇表 | 搜索 `const BUILTIN_WORDS`（改完 `BUILTIN_VERSION` 加 1） |
| 词头拆分规则 | 搜索 `function splitEntry` |
| 主题色 | 搜索 `--brand:#0f766e` |
